# Environment Configuration
# Copy this file to .env and fill in the values
# See docs/environment-variables.md for complete documentation

###############################################################################
# Database
###############################################################################
DB_NAME=mission_ki_db
DB_USER=mission_ki_user
DB_PASSWORD=mission_ki_password
DB_HOST=db
DB_PORT=5432

###############################################################################
# Application
###############################################################################
# Locale and timezone
APP_DEFAULT_LOCALE=de_DE
APP_DEFAULT_TIMEZONE=Europe/Berlin

# Debug mode (true for development, false for production)
DEBUG=true

# Security salt - REQUIRED: Generate a unique 64-character random string
# Example: openssl rand -hex 32
SECURITY_SALT=__SALT__77e8f6b0f666cd18c5f03d16bc866702bbefe46b71bfcc6766030ae6e9cca164__SALT__

###############################################################################
# Storage
###############################################################################
# Driver: "local" (default, no setup required) or "s3" (AWS/MinIO)
STORAGE_DRIVER=local

# Local storage path (relative to webroot)
STORAGE_LOCAL_PATH=storage/uploads

# S3 configuration (required when STORAGE_DRIVER=s3)
S3_BUCKET_NAME_ATTACHMENTS=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=

# Optional: Custom S3 endpoint for MinIO or S3-compatible services
# S3_ENDPOINT=http://minio:9000
# S3_USE_PATH_STYLE=true

###############################################################################
# Email
###############################################################################
# Driver: "ses" (AWS SES, default) or "smtp" (self-hosted/Mailpit)
EMAIL_DRIVER=ses

# AWS SES configuration (required when EMAIL_DRIVER=ses)
SES_REGION=eu-west-1
SES_ACCESS_KEY_ID=
SES_SECRET_ACCESS_KEY=
SES_SENDER_EMAIL=
SES_SENDER_NAME=Pruefportal
SES_NOREPLY_EMAIL=
SES_NOREPLY_NAME=Pruefportal

# SMTP configuration (required when EMAIL_DRIVER=smtp)
# For local development with Mailpit: SMTP_HOST=mailpit, SMTP_PORT=1025
# SMTP_HOST=localhost
# SMTP_PORT=1025
# SMTP_USERNAME=
# SMTP_PASSWORD=
# SMTP_TLS=false

###############################################################################
# AWS
###############################################################################
AWS_DEFAULT_REGION=eu-central-1

###############################################################################
# GitHub Integration (for bug reports)
###############################################################################
# Personal access token with repo scope
GITHUB_TOKEN=
GITHUB_REPO_OWNER=Mission-KI
GITHUB_REPO_NAME=pruefportal
